# AI 기반 한국어 광고 감성·취향 분석기
# Python 3.12 이상 필요

# 핵심 의존성
numpy>=2.3.5
rich>=13.0.0
scikit-learn>=1.7.2

# 참고: tkinter는 Python 표준 라이브러리로 별도 설치 불필요
# GUI 버전(gui_version/main_gui.py)을 사용하려면 tkinter가 시스템에 설치되어 있어야 함
# Ubuntu/Debian: sudo apt-get install python3-tk
# macOS: 기본 포함
# Windows: 기본 포함
